<div class="message-input">
  <%= form_for(@message, remote: true) do |f| %>
      <%= f.text_area :content, class: 'form-control', placeholder: 'Press enter to submit' %>
      <%= f.submit "Send" %>
  <% end %>
</div>